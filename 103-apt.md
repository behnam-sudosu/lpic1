# apt  

## debian
	wget (link) ===>> download
	dpkg -i (name of program) ===>> for install
	apt-get update
	apt-get upgrade
	apt update
	apt upgrade
	apt install openssh-service
	apt-cache search firefox ===>> show firefox in repository
	apt list --upgradable ===>> show package must upgrade
	apt-cache showpkg nginx ===>> show library
	apt-cache depends nginx ===>> show dependency
	nexus repositry
	harbor repositry
	vim /etc/apt/sources.list.d
		deb htt://IP
	apt remove (name package) ===>> remove package
	apt purge (name package) ===>> uninstall program
	maintrance ===>> give me time to change
	apt autoremove ===>> remove dependency
	do-release-upgrade ===>> upgrade your os 
	aptitude
		hold ===>> never upgrade this program
		unhold
		download

## redhat
	wget (link)
	rpm -ivh (name of application)
	winncp ===>> can transfer file from windows to linux
	rpm -q ===>> query
	rpm -l ===>> where copy file in directory
	rpm -qip ===>> show more information about package
	rpm -qc ===>> where is config file
	rpm -qa ===>> all list
	rpm -e ===>> delete package
	
	yum
	dnf ===>> install package
		/etc/yum.repos.d
	yum check-update ===>> update
	yum update
	yum install vim
	yum list ===>> show list of install in your od
	yum list installed
	yum downloader
	 
